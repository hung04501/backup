<div class="todoapp-wrapper">
  <section class="todoapp">
    <header class="header">
      <h1>todos</h1>
      <input appFrAutocomplete class="new-todo" #newTodo placeholder="What needs to be done?" autofocus>
    </header>
    <section class="main">
      <ul class="todo-list">
        <li *ngFor="let todo of todos | async" [class.completed]="todo.completed">
          <div class="view">
            <input class="toggle" type="checkbox" (click)="toggleCompleteTodo(todo.id, !todo.completed)" [checked]="todo.completed">
            <label>{{ todo.content }}</label>
            <button class="destroy" (click)="deleteTodo(todo.id)"></button>
          </div>
        </li>
      </ul>
    </section>
  </section>
</div>

<div>
  <div>Current Count Value: {{ counterFromStore | async }}</div>
  <a href="#" (click)="increment()">Increment</a>
  <a href="#" (click)="decrement()">Decrement</a>
  <a href="#" (click)="reset()">Reset</a>
</div>
